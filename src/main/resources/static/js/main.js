/* This is just an example, after we will replace this alert with real
AJAX calls or UI logic (for example, loading building data)  */

document.addEventListener('DOMContentLoaded', () => {
    const button = document.getElementById('newQuoteBtn');
    button.addEventListener('click', () => {
        alert('Welcome to Syndico App — your smart syndic management system!');
    });
});
